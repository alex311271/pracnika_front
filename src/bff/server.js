import {
	authorize,
	logout,
	register,
	fetchRoles,
	fetchUsers,
	updateUseRole,
} from './operations';

export const server = {
	authorize,
	logout,
	register,
	fetchUsers,
	fetchRoles,
	updateUseRole,
};
